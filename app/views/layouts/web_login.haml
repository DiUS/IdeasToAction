!!! 5
%html(lang="en")
  %head
    %meta(charset="utf-8")
    %meta(http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1")
    %meta(name="viewport" content="width=device-width, initial-scale=1.0")
    %title= content_for?(:title) ? yield(:title) : "Web Login"
    = csrf_meta_tags
    = stylesheet_link_tag "active_admin", :media => "all"
  %body.active_admin.logged_out.new
    #wrapper

      #content_wrapper
        .flashes
          - flash.each do |key, value|
            = content_tag(:div, value, :class => "flash flash_#{key}")

        #active_admin_content
          #login
            = yield