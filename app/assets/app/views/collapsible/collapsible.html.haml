.collapsible(ng-class="{collapsed : !expanded, expanded: !!expanded}")
  .header(ng-click="expandCollapse()" ng-class="{fixed: canCollapse == false}")
    %span<
      {{title}}
      %sup><
        {{collection_length}}
  .items(ng-show="!!expanded")
    .item(ng-repeat="item in collection")
      %ng:include(src="itemTemplate")
    .clear(ng-transclude)
