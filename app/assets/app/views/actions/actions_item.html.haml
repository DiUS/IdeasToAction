%p.description(ng-href="/ideas/{{item.idea_id}}")
  {{item.description}}
  %br(ng-show='item.completion_date')
  %em(ng-show='item.completion_date') Completed on {{item.completion_date | date:'short'}}
